---
/**
 * Example Project Scenarios — 3 before/after cards.
 * NOT called "case studies" — clearly labelled as examples.
 * Per plan v3: replace with real client stories when available.
 */
import Container from '../ui/Container.astro';
import Section from '../ui/Section.astro';
import Badge from '../ui/Badge.astro';
import Icon from '../ui/Icon.astro';
import { getContent } from '../../lib/i18n';
import type { Locale } from '../../content/types';

interface Props {
  locale?: Locale;
}
const { locale = 'en' } = Astro.props;
const { exampleScenarios } = getContent(locale).examples;
---

<Section id="examples" bg="canvas">
  <Container size="xl">
    <div class="mx-auto max-w-3xl text-center" data-reveal>
      <div class="flex items-center justify-center gap-2">
        <h2 class="text-3xl font-bold tracking-tight text-ink md:text-4xl">
          Example project scenarios
        </h2>
      </div>
      <p class="mt-4 text-lg text-ink-3">
        Realistic examples of the kinds of problems we solve. Not real client
        names — labelled as examples until we have permission to publish real
        work.
      </p>
      <div class="mt-4 flex justify-center">
        <Badge variant="warn">
          <Icon name="info" size={12} />
          Illustrative examples
        </Badge>
      </div>
    </div>

    <div class="mt-16 grid gap-6 lg:grid-cols-3">
      {
        exampleScenarios.map((scenario) => (
          <div class="flex flex-col rounded-lg border border-line bg-surface shadow-card">
            {/* Header */}
            <div class="border-b border-line bg-surface-2 px-6 py-4">
              <h3 class="text-base font-semibold text-ink">
                {scenario.sector}
              </h3>
            </div>

            {/* Problem */}
            <div class="px-6 pt-5">
              <div class="flex items-center gap-2 text-xs font-semibold uppercase tracking-wider text-danger">
                <Icon name="close" size={14} />
                Problem
              </div>
              <p class="mt-2 text-sm leading-relaxed text-ink-3">
                {scenario.problem}
              </p>
            </div>

            {/* Solution */}
            <div class="px-6 pt-5">
              <div class="flex items-center gap-2 text-xs font-semibold uppercase tracking-wider text-brand">
                <Icon name="code" size={14} />
                Solution
              </div>
              <p class="mt-2 text-sm leading-relaxed text-ink-2">
                {scenario.solution}
              </p>
            </div>

            {/* Result */}
            <div class="mt-auto px-6 pb-6 pt-5">
              <div class="flex items-center gap-2 text-xs font-semibold uppercase tracking-wider text-ok">
                <Icon name="check" size={14} />
                Result
              </div>
              <p class="mt-2 text-sm leading-relaxed text-ink-2">
                {scenario.result}
              </p>
            </div>
          </div>
        ))
      }
    </div>
  </Container>
</Section>
