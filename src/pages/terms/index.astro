---
import LegalLayout from '../../layouts/LegalLayout.astro';
import { getContent } from '../../lib/i18n';

const locale = 'en' as const;
const { terms } = getContent(locale).legal;
---

<LegalLayout content={terms} path="/terms/" locale={locale} />
